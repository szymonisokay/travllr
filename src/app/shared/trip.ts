export class Trip {
    constructor(public id: number, public imgPath: string, public name: string,
        public price: number, public dateFrom: string, public dateTo: string, public desc: string, public qty: number) {

    }
}